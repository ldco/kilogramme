// tree-sitter grammar for TypeScript/TSX.
// Currently all rules use text-based analysis. tree-sitter integration planned.
//
// import Parser from 'tree-sitter'
// import TypeScript from '@tree-sitter/typescript'
//
// const parser = new Parser()
// parser.setLanguage(TypeScript)
// export function createParser(): Parser { return parser }

export function createParser(): null {
  return null
}
